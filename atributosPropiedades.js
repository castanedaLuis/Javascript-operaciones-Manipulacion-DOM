/* Estas son los atributos /o Propiedades
    palceholder
    requiered
    id
    class
    type
    value <----- valor del nodo

    atributo !== propiedad
    ATRIBUTO -> son utilizados al inicio de la etiqueta HTML type, class, id, placeholder, required, etc
    PROPIEDADES -> Se ve reflejado en el DOM pero no en el HTML  P/E h3.value ='cambio solo se vera en el dom y no en el html'

    Las propiedades pueden cambiar en cualquier momento pero los atributos 
    solo son utilizados al principio y para inicializar el HTML
*/
